import {
  "moonbitlang/core/json",
  "moonbitlang/core/double",
  "moonbitlang/core/strconv",
  "moonbitlang/x/sys",
  "moonbitlang/x/fs",
}
// get_source is pkg-private, used in whitebox tests and Improvement 3.
// moon build excludes _wbtest.mbt, so suppress false-positive unused_value.

warnings = "-1"
